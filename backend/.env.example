PORT=4000
FRONTEND_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,https://joelyk.github.io
# TRUST_PROXY=1
# API_RATE_LIMIT=120
# AI_RATE_LIMIT=30
# CONTACT_RATE_LIMIT=10

# Optional: persist contact leads to a JSON file
# LEADS_PERSIST_PATH=storage/leads.json

# Optional: persist image overrides used by the admin area
# CONTENT_OVERRIDES_PATH=storage/content-overrides.json

# Admin access for /api/admin/login, /api/admin/content and /api/admin/leads
# ADMIN_API_KEY=change_me
# ADMIN_SESSION_SECRET=change_me_too
# ADMIN_SESSION_TTL_HOURS=12

OPENAI_API_KEY=your_openai_key
OPENAI_MODEL=gpt-4o-mini
