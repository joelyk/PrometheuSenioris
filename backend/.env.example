PORT=4000
FRONTEND_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,https://joelyk.github.io
# TRUST_PROXY=1
# API_RATE_LIMIT=120
# AI_RATE_LIMIT=30
# CONTACT_RATE_LIMIT=10

# Optional: persist contact leads to a JSON file (relative to backend/ when run via workspace scripts)
# LEADS_PERSIST_PATH=storage/leads.json

# Optional: protect admin endpoint /api/admin/leads
# ADMIN_API_KEY=change_me
OPENAI_API_KEY=your_openai_key
OPENAI_MODEL=gpt-4o-mini
